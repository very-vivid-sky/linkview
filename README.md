# Linkview
## ...but what if we can improve what pasted links look like?

Cy Bautista's submission to Mirothon

## Setting up
1. run `npm run start` in the frontend folder
2. run `npm run start` in the backend folder
3. launch http://localhost:3000
4. launch miro